services:
  juice-shop-secrets:
    image: kiranbazooka/juice-shop:01  # Your existing image
    ports:
      - "3000:3000"
    environment:
      - AWS_SECRET=AKIAVULNERABLE1234567890
      - DB_PASS=JuiceShopAdmin2025!
      - API_KEY=sk-live-vulnerable123456789
    volumes:
      - ./host-secrets:/juice-shop/host-secrets:ro
    secrets:
      - db_creds
    privileged: true

secrets:
  db_creds:
    file: ./secrets/db_password.txt
